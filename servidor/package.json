{
  "name": "server",
  "version": "0.1.0",
  "description": "PaymenComponent ejemplo con stripe",
  "main": "Servidor.js",
  "scripts": {
    "start": "node Servidor.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"**/*.{js,jsx}\""
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "stripe": "^8.191.0"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}